import { Text } from 'react-native-paper';
import { TextProps } from 'react-native-paper/lib/typescript';

const InputLabel = (props: TextProps<never>) => <Text {...props} variant="bodyLarge" />;

export default InputLabel;
